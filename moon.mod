name = "kawaz/timespec"

version = "0.2.1"

readme = "README.md"

repository = "https://github.com/kawaz/timespec.mbt"

license = "MIT"

keywords = [ "time", "datetime", "duration", "range", "span", "cli", "parser" ]

description = "CLI-oriented time specification parser for MoonBit"

options(
  source: "src",
)
